def educational_answer(prompt: str) -> str:
    return (
        "AI helper integration is intentionally left as a service boundary for future expansion. "
        f"For now, try /learn or /glossary for: {prompt}"
    )
